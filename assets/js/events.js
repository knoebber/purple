window.addEventListener('purple:hamburger', ({ target }) => {
  target.classList.toggle('hidden');
});

window.addEventListener('purple:hamburger', ({ target }) => {
  target.classList.toggle('hidden');
});
