<% path = Routes.items_path(@conn, :update_completed_at, @item.id) %>
<%= if @item.completed_at do %>
  <%= link("Set Incomplete", to: path, method: :delete) %>
<% else %>
  <%= link("Set Complete", to: path, method: :put) %>
<% end %>
